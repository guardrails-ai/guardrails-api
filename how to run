1. Go to AWS dev act
2. Everything is in US-west-2
3. Code build - ‘grass-lite-demo-build’
4. Start build

Repo - guardrails-api/
Manifests are in custom-install/manifests
Dockerfile.lite
Lite-build.sh
Docker run - lite-run.sh


pyenv exec python -m venv .venv
source .venv/bin/activate

make install-lock

python custom-install/install.py

#optionally, run `guardrails hub install ...`

#run infra
# guardrails-dev repo telemetry branch
git submodule update --init --recursive;
./lite.sh infra-lite;

bash local.sh

